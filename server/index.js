// const { NextExpressServer } = require('@brendanatme/next-express-server');

// const port = process.env.PORT || 3000;

// const server = new NextExpressServer({
//   middlewares: [

//   ],
// });

// server.listen(port).then(() => console.log('server listing on http://localhost:' + port));
